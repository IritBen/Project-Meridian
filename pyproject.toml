[project]
name = "project-meridian"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "psycopg[binary]>=3.3.5",
]

[dependency-groups]
dev = [
    "docker>=7.2.0",
    "pytest>=9.1.1",
]
